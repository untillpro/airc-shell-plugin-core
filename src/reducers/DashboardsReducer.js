const INITIAL_STATE = {
    loading: false,
    data: [],
    from: null,
    to: null,
    settings: {},
};

const reducer = (state = INITIAL_STATE, action) => {
    return state;
}

export default reducer;